import { User } from "../../../../shared/types";
import { Border, Container } from "./styles";

export const IconBlank = () => {
  return (
    <Border>
      <Container>
      </Container>
    </Border>
  );
}